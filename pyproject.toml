[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "virtual-try-on-platform"
version = "0.1.0"
description = "AI-powered 3D Virtual Try-On & Fit Recommendation Platform"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
